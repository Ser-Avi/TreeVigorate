#pragma once

// using namespace UniEngine; //commmented out by Avi
namespace EcoSysLab {
	// all of this is commented out by Avi, since we won't be using a Sorghum Model anyway, and it relies on
	// UniEngine things, which we won't need
//class CBTFGroup : public IAsset{
//public:
//  std::vector<AssetRef> m_doubleCBTFs;
//  void OnInspect() override;
//  void CollectAssetRef(std::vector<AssetRef> &list) override;
//  void Serialize(YAML::Emitter &out) override;
//  void Deserialize(const YAML::Node &in) override;
//  AssetRef GetRandom() const;
//};
}